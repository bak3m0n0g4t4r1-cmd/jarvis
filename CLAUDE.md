# CLAUDE.md — Джарвис (локальный голосовой пульт)

> Этот файл — постоянный контекст для Claude Code. Читается каждую сессию.
> Описывает не только структуру, но и ПРИНЯТЫЕ РЕШЕНИЯ и ГРАБЛИ — чтобы не
> предлагать отвергнутое и не повторять ошибок.

---

## ЧТО ЭТО

«Джарвис» — **простой, надёжный, автономный голосовой пульт** на базовые команды:
event-driven микросервисы через MQTT. Полностью локальный, без интернета.

Так было НЕ всегда: раньше это был «умный ассистент» с облаком (Gemini + function
calling + локальная 1.5B как диспетчер). От облака/LLM **отказались осознанно** —
N100/8 ГБ не тянет умные модели, система тупит. Цель теперь — **минимум CPU/RAM**
при точном распознавании произвольно сформулированных команд. Характер Джарвиса
(ответы-подтверждения «в характере», «Сделано, сэр») СОХРАНЁН — пульт ≠ безликий робот.

**Язык проекта — русский.** Код, комментарии, логи, ответы ассистента, сообщения
коммитов — на русском. Общение с пользователем — на русском.

---

## ЖЕЛЕЗО И ОКРУЖЕНИЕ (важно для всех решений)

- **Intel N100 (4 ядра), без GPU, 8 ГБ RAM, swap = 0** — слабое железо, RAM впритык.
  На TUXEDO swap НЕ настроен (на Kali был) → OOM-риск выше, всё должно быть лёгким
  (рекомендация — включить zram, см. ГРАБЛИ). Первопричина перехода в локальный пульт.
- **TUXEDO OS 24.04** (Ubuntu-база), KDE Plasma 6 (Qt 6.9), ядро 6.17, **Wayland**, **zsh**.
  Раньше была Kali — окружение сменилось, бинарии сверены под TUXEDO (см. ГРАБЛИ/commands.yaml).
- Пользователь занимается пентестом — НЕЛЬЗЯ гнать системный трафик через прокси/VPN.
- Проект: `/home/tux/proj/jarvis` (пользователь `tux`). Гитхаб: `github.com/stsarapkinser-arch/jarvis_data-bus`.
- venv в `.venv` (`source .venv/bin/activate`). PEP 668 — pip ТОЛЬКО в venv, без `--break-system-packages`.
- Интернета в работе НЕ требуется (нужен лишь разово — скачать модели).

---

## АРХИТЕКТУРА (полностью локальная, без облака)

Голос → распознавание двумя дешёвыми слоями → тег команды → исполнение. Никакой
генеративной модели: ум заменён на правила + лёгкие эмбеддинги.

```
Пользователь говорит «Джарвис, …»
   │
   ▼
STT «Уши» (sherpa-onnx: silero-VAD + zipformer-ru) → текст после wake-word → jarvis/input
   │
   ▼
CORE «Мозг» (jarvis/services/core.py — тонкий сервис)
   ├─ встроенные info-ответы (время/заряд) — офлайн, мгновенно, в характере
   ├─ matcher.match(text)  (jarvis/matcher.py, источник истины — commands.yaml):
   │     1) СЛОЙ ПРАВИЛ (мгновенно, ноль моделей): синонимы + difflib-fuzzy.
   │        Направление/полярность (вкл/выкл, громче/тише) задаёт ТОЛЬКО он.
   │     2) СЛОЙ ЭМБЕДДИНГОВ (если правила промахнулись): rubert-tiny2 ONNX,
   │        косинус к примерам-фразам, порог + защита по отрыву (margin).
   │  → тег → say(подтверждение в характере) + jarvis/execute {command_tag}
   └─ не распознано → say("Боюсь, не разобрал, сэр. Повторите?")
                                          │
                                          ▼
                        OS-агент «Руки» (commands.yaml, shell=False, allow-list)
                                          │
                                          ▼
                                  TTS «Голос» (Piper) ← jarvis/say
```

**Почему так:** команды звучат мгновенно, работают офлайн, ничего не тупит. Команды
вроде «выключи Wi-Fi» исполняются локально и не рвут себе канал (облака больше нет вовсе).

---

## СТЕК (работает на железе, подтверждено `jarvis doctor`)

- **STT «Уши»**: sherpa-onnx, **zipformer-ru** (`sherpa-onnx-small-zipformer-ru-2024-09-18`,
  offline transducer, int8). НЕ SenseVoice (без русского). silero-VAD. Захват
  sounddevice 16кГц моно. Wake-word «джарвис» через VAD+ASR + difflib (ловит «джарвиз/сервис»).
  VAD-пороги ЗАКРЕПЛЕНЫ: `JARVIS_VAD_THRESHOLD=0.3`, `JARVIS_VAD_MIN_SILENCE=0.8` — НЕ ТРОГАТЬ.
  Лёгкость: `VAD_BUFFER_SECONDS=10` (было 30), `STT_NUM_THREADS=1` (sherpa — один поток; сверено:
  1 поток БЫСТРЕЕ 2/3 на этой модели). **Push-to-talk** (Этап 14): зажатие правого Ctrl → команда без
  wake-word (читаем /dev/input; ducking музыки; см. ГРАБЛИ). Замер TUXEDO: RSS ~115 МБ (модели),
  CPU ~4–5% одного ядра при прослушивании в покое.
- **Матчер (распознавание)**: `jarvis/matcher.py` — правила (синонимы из `commands.yaml`
  + difflib) + автономный эмбеддер **rubert-tiny2** (ONNX через onnxruntime, БЕЗ torch;
  токенизатор — `tokenizers`). Пулинг **mean** (сверено: разделяет лучше CLS),
  L2-норма. Модель грузится ЛЕНИВО (при первом промахе правил, ~0.3с), эмбеддинги
  команд кешируются в памяти и на диск (`cmd_emb_cache.npz`). На TUXEDO процесс с эмбеддером
  ~190 МБ RSS (≈ +165 МБ к core) — поэтому ленив; на правилах он не нужен.
- **TTS «Голос»**: Piper 1.4.2, `ru_RU-dmitri-medium.onnx` (22050 Гц). API: `synthesize(text,
  syn_config=SynthesisConfig(length_scale=…))` → итератор AudioChunk (`audio_int16_*`). НЕ старый
  `synthesize_stream_raw`. Скорость — `voice.length_scale`; тон — `voice.pitch` (Piper pitch не
  умеет → подмена частоты `pw-cat --rate` + компенсация темпа, см. ГРАБЛИ).
  Голос грузится ЛЕНИВО при первой фразе (на TUXEDO: idle TTS ~40 МБ, с Piper ~162 МБ).
  **ВОСПРОИЗВЕДЕНИЕ — через PipeWire `pw-cat` (subprocess), НЕ sounddevice/PortAudio**: на
  TUXEDO PortAudio видит только HDMI-выходы → была немота (см. ГРАБЛИ). `JARVIS_TTS_SINK`
  переопределяет sink (пусто = системный default). STT-захват остаётся на sounddevice.
- **OS-агент «Руки»**: исполняет теги из `commands.yaml`. `shell=False`, allow-list,
  без произвольного исполнения.

Зависимости (минимум): `paho-mqtt`, `pyyaml`, `numpy`, `sounddevice`, `onnxruntime`,
`tokenizers`, `sherpa-onnx`, `piper-tts`. НЕТ: google-genai, ollama, pydantic,
python-dotenv (удалены). `httpx`/`protobuf` остаются только как транзитивные
(huggingface-hub у tokenizers; protobuf у onnxruntime) — не трогать.

---

## РАСКЛАДКА КОДА

```
/home/tux/proj/jarvis/
├── pyproject.toml            # entry points: jarvis, jarvis-stt, jarvis-core, jarvis-os-agent, jarvis-tts, jarvis-scheduler
├── settings.yaml             # ★ ЕДИНЫЙ файл ВСЕХ настраиваемых параметров (секции + русские комментарии)
├── schedule.yaml             # ★ расписание будильников (человекочитаемый; правится голосом и вручную)
├── commands.yaml             # тег → {описание, команда:[...], ждать_вывод, синонимы:[...], примеры:[...], подтверждение(строка|пак)}
├── models.yaml               # карта моделей для загрузчика (zipformer, piper, эмбеддер rubert-tiny2)
├── .env / .env.example       # пусто; настройки теперь в settings.yaml (env JARVIS_* — опц. оверрайд)
├── jarvis/
│   ├── bus.py                # JarvisModule: базовый класс (MQTT paho-2.x VERSION2, reconnect+backoff,
│   │                         #   RotatingFileHandler, heartbeat, graceful shutdown, say/publish_json/set_state)
│   ├── config.py             # ЕДИНАЯ загрузка settings.yaml (приоритет env→yaml→дефолт); BASE_DIR=корень
│   ├── contracts.py          # топики + QoS + State + Intent=Literal["os_command"] (чистый typing, без pydantic)
│   ├── matcher.py            # распознавание: правила + ONNX-эмбеддинги (rubert-tiny2)
│   ├── sysinfo.py            # read-only пробы системы (заряд, загрузка CPU/RAM, громкость)
│   ├── speech.py             # числа/время/дата/температура словами для TTS (say_time/say_clock/say_temperature)
│   ├── phrases.py            # ★ общий выбор фраз без повторов в цикле (pick/reset) — для ВСЕХ паков
│   ├── audio_env.py          # адаптивная громкость: реальные RMS (микрофон+monitor), ducking
│   ├── resilience.py         # диагнозы сбоёв MQTT (классификатор + расшифровка разрыва)
│   ├── breaks.py             # стоп-фразы перерыва (difflib) — гейт is_stop_phrase для core/монитора
│   ├── alarms.py             # будильники: is_alarm_command (гейт) + парсер времени/команд + I/O schedule.yaml
│   ├── timers.py             # таймеры/секундомеры: parse_duration + гейты + парсеры команд (reuse alarms)
│   ├── worldtime.py          # мировое время: город→tz (geocode+zoneinfo) + разница с регионом
│   ├── reminders.py          # напоминания/задачи: парсер дат + команд + гейты + диалог дозапроса
│   ├── chains.py             # цепочки: ветки продолжений + комбо-split + обратимость + история + гейты
│   ├── silence.py            # режим тишины: гейт «без звука»/«включи звук» + состояние-файл (персист)
│   ├── notify.py             # системные уведомления (D-Bus/gdbus) + кнопка-действие «Открыть логи»
│   ├── weather.py            # погода Open-Meteo (геокодинг кириллицей + кэш, best-effort, таймаут)
│   ├── cli.py, doctor.py, downloader.py, ui.py, services_map.py
│   └── services/             # stt.py, core.py, os_agent.py, tts.py, activity_monitor.py, scheduler.py — наследники JarvisModule, main()
├── systemd/                  # --user юниты, Restart=always, MemoryAccounting=true, ExecStart=абс. путь venv
├── models/                   # .gitignore (zipformer-ru, piper-голос, rubert-tiny2-onnx + кеш эмбеддингов)
└── logs/
```

**MQTT-топики:** `jarvis/input` {"text"} · `jarvis/execute` {"command_tag"} QoS1 ·
`jarvis/say` {"text","source"} · `jarvis/state` {"state": idle|listening|thinking|speaking}.

**Настройки** = ОДИН файл `settings.yaml` в корне (секции voice/adaptive_audio/hearing/
recognition/startup/system/models/break_reminders/местоположение/alarms/timers/worldtime/coin/reminders/chains/notifications/silence, русские комментарии к каждому параметру). Код читает ТОЛЬКО через
`config.py` (единая загрузка: приоритет env `JARVIS_*` → `settings.yaml` → дефолт; отсутствие
файла/параметра → дефолт, не падает). Менять параметр = править `settings.yaml`, Python не трогать.
VAD-пороги (0.3/0.8) и анти-эхо в файле помечены ⚠ «настроено опытным путём».

**Лего-модульность:** новые модули наследуют `JarvisModule` из `bus.py`. Не дублировать
MQTT/логирование/shutdown — всё в базовом классе.

**Добавить команду** = блок в `commands.yaml` (тег + `синонимы` + `примеры` +
`подтверждение`), Python не трогать. `подтверждение` — СТРОКА или СПИСОК-пак вариаций (выбор без
повторов через `jarvis/phrases.py`). Направление/полярность задавай явными синонимами
с глаголом — эмбеддинги антонимы НЕ различают.

---

## БЕЗОПАСНОСТЬ (жёсткие правила)

- **НЕТ произвольному shell.** Матчер выдаёт только ТЕГ из allow-list `commands.yaml`;
  пользовательский текст в команду не подставляется. Исполняет OS-агент через subprocess
  `shell=False` строго списком аргументов.
- **Необратимые действия** (удаление, выключение, разрыв сети) — только безопасные/
  обратимые в `commands.yaml`. Без root-деструктива/атак.
- Зрения/скриншотов-в-облако нет (облако удалено целиком).

---

## ДОРОЖНАЯ КАРТА

- **Этап 1 — ✅ СДЕЛАНО.** Вычистка облака/LLM (Gemini, прокси, ротация ключей, casual,
  function calling, Ollama/qwen). Новое распознавание: правила + ONNX-эмбеддинги.
- **Этап 2 — ✅ СДЕЛАНО.** Оптимизация под минимум ресурсов: ленивый Piper (~106 МБ в
  простое), ленивый эмбеддер + диск-кеш, VAD-буфер 30→10с, `num_threads=1`, чистка
  мёртвого кода (`with_retry`, `JarvisResponse`+pydantic) и осиротевших пакетов.
- **Этап 3 (чекап на TUXEDO) — ✅ СДЕЛАНО.** Переезд Kali→TUXEDO OS 24.04 (Ubuntu-база):
  пересоздан `.venv`, поставлены mosquitto/brightnessctl, `browser`→`thorium-browser`, удалён
  мёртвый sense-voice (1,1 ГБ), починены зомби-процессы OS-агента и нестабильный выбор
  подтверждений (`hash()`→стабильный), вычищены хвосты (Ollama/SenseVoice/Kali). `jarvis doctor` зелёный.
- **Этап 4 (немота + команды) — ✅ СДЕЛАНО.** Причина немоты: sounddevice/PortAudio играл в
  HDMI (22050 Гц не поддержан) → исключение глоталось. Фикс: воспроизведение через PipeWire
  `pw-cat`. Терминал→kitty. `commands.yaml` расширен до 39 команд (звук/яркость уровни, медиа
  `playerctl`, скрины окно/область/буфер, рабочие столы KWin-DBus, сон, буфер `wl-clipboard`,
  приложения KDE6). Info-ответы: дата/громкость/CPU-RAM. `jarvis doctor` зелёный.
- **Этап 5 (вширь+вглубь + произношение) — ✅ СДЕЛАНО.** commands.yaml → 70 команд: сайты
  (Thorium URL: ютуб/диск/фото/карты/gemini/claude), Я.Музыка + «моя волна», XDG-папки (русские
  имена), носители по метке `/media/tux/<МЕТКА>` (поле `требует_путь` → ответ в характере, если не
  подключён), вкладки браузера (ydotool, Wayland), VPN Amnezia (только запуск GUI), LibreOffice/
  gparted, ночной режим (kglobalaccel «Toggle Night Color»). Новый `speech.py`: время/дата/проценты
  словами («четырнадцать часов тридцать минут»). `jarvis doctor` зелёный.
- **Этап 6 (адаптивная громкость + скорость) — ✅ СДЕЛАНО.** Громкость голоса как ОСНОВА TTS: по
  РЕАЛЬНЫМ уровням сигнала (микрофон + monitor PipeWire, не ползунок), ducking музыки, следование
  за внешним шумом И громкостью речи пользователя (тихо→шёпот, шум→разборчивость), усиление >100%
  (gain PCM). Новый `audio_env.py`. Ускорение: matcher правила 24→0.003 мс (dict точных синонимов),
  прогрев Piper при старте (первая фраза без 3с), потоковый синтез→pw-cat. `jarvis doctor` зелёный.
- **Этап 7 (единый файл настроек) — ✅ СДЕЛАНО.** Все ~50 параметров сведены в один
  человекочитаемый `settings.yaml` (секции voice/adaptive_audio/hearing/recognition/system/
  models, русские комментарии); `config.py` грузит через `_get` (приоритет env→yaml→дефолт);
  нет файла/параметра или битый YAML → дефолты (не падает). Базовая громкость 0.7→0.85. doctor
  проверяет валидность settings.yaml.
- **Этап 8 (фикс регрессии путей + глубокий аудит) — ✅ СДЕЛАНО.** Регрессия Этапа 7: пути в
  settings.yaml относительные → под systemd не находились (STT падал, остальные «зомби-живые»).
  Фикс: `config.py` резолвит ВСЕ пути от `BASE_DIR` (`_get_path` → абсолютные); `WorkingDirectory`
  в юнитах; `doctor` FAIL'ит относительные пути (был слеп). Аудит всего проекта: миграция значений
  сверена с git (без потерь, VAD 0.3/0.8 целы), подключён осиротевший `VOLUME_RAMP` в `audio_env`,
  точные заметки на 7 команд. Контракты/анти-эхо/bus/matcher проверены — целы. `jarvis doctor` зелёный.
- **Этап 9 (детектор активности + напоминания о перерыве) — ✅ КОД ГОТОВ (живая проверка за
  пользователем).** Новый сервис `activity_monitor` (наследник JarvisModule, needs_audio=False):
  следит за активностью, по случайному циклу 20–30 мин предлагает перерыв «в характере», при
  игноре через 5–6 мин плавно затемняет экран и повторяет, за перерыв 4–5 мин хвалит, по
  стоп-фразе отвечает и сбрасывает цикл. **Idle на KDE6/Wayland опросить нельзя** (сверено:
  GetSessionIdleTime → NotSupported, logind idle мёртв) → читаем `/dev/input` (чисто-входной
  простой: фильм/музыка без касания = простой растёт, инхибиторы игнорируются; устройства
  фильтруем по EV-битам из `/proc/bus/input/devices`). Яркость — `brightnessctl` (плавная рампа,
  без OSD, state-файл `logs/break_dim_state.json` переживает рестарт). Все ~18 параметров + 10+10+3
  фраз — секция `break_reminders` в settings.yaml. Минимальный хук в core (не отвечать «не разобрал»
  на стоп-фразу). doctor: `check_input_access` (+группа input) и `check_brightness`. `jarvis doctor`
  зелёный (✗0). **ПРЕДУСЛОВИЕ от пользователя:** `sudo usermod -aG input tux` + ПЕРЕЛОГИН (без этого
  сервис «спит», doctor WARN'ит), затем `jarvis start`.
- **Этап 10 (голос: речь/фразы/адаптивный детект + диагностика) — ✅ КОД ГОТОВ (живая проверка
  за пользователем).** (1) Речь ЧУТЬ медленнее и ниже: `voice.length_scale` (1.12) + `voice.pitch`
  (0.95) в settings.yaml — НЕЗАВИСИМЫ (Piper не умеет pitch → подмена частоты pw-cat + компенсация
  темпа). (2) ФУНДАМЕНТ — общий механизм фраз без повторов `jarvis/phrases.py` (`pick(key, pak)`):
  цикл без повторов, по исчерпании обнуляется; на него переведены подтверждения, стартовые фразы И
  фразы перерыва (`breaks.pick_phrase` удалён). (3) Стартовое объявление по статусу: `cmd_start`
  ждёт осадки сервисов, по `is-active` всех юнитов выбирает пак `startup.success/problem_phrases` и
  озвучивает (раньше старт молчал; «каша» была от `jarvis doctor`). (4) Подтверждения: `подтверждение`
  в commands.yaml теперь СТРОКА или СПИСОК-пак (20 типовых команд → паки 3–4), общий пул расширен до 8.
  (5) Адаптивный порог VAD: в ТИШИНЕ чуть ниже базы (0.3→0.22, ловим тихий онсет/wake-word с первого
  раза), в шуме — закреплённая база; шумовой пол меряется по СВОЕМУ потоку STT, VAD пересобирается при
  смене полосы. (6) Тихо услышал→тихо ответил — уже было (user_level), сверено. **Диагностика повторов
  (по логам, не гадание):** тихое начало фразы не открывало VAD → wake-word терялся → команда молча
  отбрасывалась (в логах «ой файлы» без публикации; удачные ретраи при user_level 0.017–0.025) — лечит
  адаптивный порог. `jarvis doctor` зелёный (✗0).
- **Этап 11 (будильники: утренний + обычные) — ✅ КОД ГОТОВ (живая проверка за пользователем).**
  Новый сервис `scheduler` (наследник JarvisModule, needs_audio=False): слушает `jarvis/input`
  ПАРАЛЛЕЛЬНО с core, сам распознаёт/исполняет команды будильника и владеет состоянием; core молчит
  на них через хук `alarms.is_alarm_command` (как `breaks.is_stop_phrase`). Команды — слой ПРАВИЛ
  (ключевые слова + difflib, НЕ эмбеддинги: поставь/отмени — антонимы). `jarvis/alarms.py`: гейт +
  `parse_time` (русский: цифры/«ЧЧ:ММ»/словами/утра-вечера/полвосьмого/без четверти/четверть/час дня
  — 28/28 тестов) + `parse_command` (действие/тип/метка) + атомарный I/O `schedule.yaml`. УТРЕННИЙ —
  singleton (ежедневный), будит ГРОМКО (поле `min_volume` в say обходит «тихо→тихо», опц. нарастание
  тихо→громко). ОБЫЧНЫЕ — множественные, разовые, метки («с пометкой X»), «убери все» (только обычные),
  предложение метки при 2-м без метки. Богатая утренняя фраза: доброе утро + время + погода
  (`jarvis/weather.py`, Open-Meteo по региону кириллицей, кэш координат, таймаут; нет сети → без
  погоды). Тик-цикл ~15с (wall-clock), grace для пропущенных (сон ноутбука), анти-двойное —
  `logs/scheduler_state.json`; всё переживает перезапуск (читается из `schedule.yaml`). Озвучка —
  `speech.say_clock`/`say_temperature` (естественно: «семь часов утра», «минус три градуса»). Регион —
  секция `местоположение/регион` в settings.yaml; ~25 параметров+паков — секция `alarms`. doctor:
  `check_alarms` (расписание/парсер/погода). `jarvis doctor --quick` зелёный (✗0). **ПРЕДУСЛОВИЕ:**
  `pip install -e .` (регистрация `jarvis-scheduler`) + `jarvis start`. Сервисов теперь 6.
- **Этап 12 (таймеры/секундомеры/мировое время/монетка) — ✅ КОД ГОТОВ (живая проверка за
  пользователем).** Расширение сервиса `scheduler` (без новых сервисов/entry points). ТАЙМЕР
  (обратный отсчёт) и СЕКУНДОМЕР (счёт вверх) — в том же `schedule.yaml` (ключи `таймеры`/
  `секундомеры`); множественные, метки, «удали все» РАЗДЕЛЬНО по типам, переживают перезапуск
  (таймер — поле `окончание`, секундомер — `старт`/`стоп`; истёкший во время простоя таймер
  срабатывает при старте). `jarvis/timers.py`: `parse_duration` (русский: «пять минут»/«полторы
  минуты»/«час двадцать»/«полтора часа»/голые единицы — 20/20 тестов) + гейты
  `is_timer/stopwatch/scheduler_command` + парсеры команд (слой правил, антонимо-безопасно). Таймер
  срабатывает ЧАЙМОМ (синус-тон в TTS, поле `chime` в say) + голосом на `timer_volume`. МИРОВОЕ ВРЕМЯ
  (`jarvis/worldtime.py`) и МОНЕТКА — мгновенные info-ответы в core (не stateful): «сколько времени в
  <город>» → geocode города (Open-Meteo, падежи→именительный кандидатами) → `zoneinfo` время+летнее+
  разница от РЕГИОНА; монетка — random+пак. core молчит на команды планировщика через
  `timers.is_scheduler_command` (раньше `is_alarm_command`). Озвучка длительности — `speech.say_duration`
  (склонения, нулевые компоненты опускаются). ~50 параметров+паков — секции `timers`/`worldtime`/`coin`.
  doctor `check_alarms` расширен (парсер длительности/таймера, мировое время, монетка). `jarvis doctor
  --quick` зелёный (✗0). Сервисов по-прежнему 6 (scheduler расширен).
- **Этап 13 (напоминания и задачи) — ✅ КОД ГОТОВ (живая проверка за пользователем).** Расширение
  `scheduler` (без новых сервисов). НАПОМИНАНИЕ (срабатывает) и ЗАДАЧА (пункт списка дел, опц. дедлайн)
  — в `schedule.yaml` (ключи `напоминания`/`задачи`); CRUD, метки, «удалить все» РАЗДЕЛЬНО, переживают
  перезапуск (пропущенное при простое срабатывает при старте). `jarvis/reminders.py`: `parse_date`
  (сегодня/завтра/послезавтра/через N дней-недель-месяцев/в пятницу/в следующий вторник/15 июня/
  пятнадцатого июня/15-го/в начале июля — 16/16 тестов) + `parse_when` (дата+время, не путает день
  «15 июня» с временем) + `extract_text` («о чём») + гейты + диалог-флаг. ТРИ режима напоминаний:
  (1) дата+время → точное срабатывание; (2) без времени СЕГОДНЯ → через random 1.5–3ч, будущая дата →
  random в дневном окне 10–18 (config); (3) неполное «напомни» → МНОГОШАГОВЫЙ диалог (спросить о чём/
  когда, собрать, подтвердить) — core молчит на ответ через общий флаг `logs/dialog_state.json` (таймаут
  45с + «отмена»). Повтор: базово «ежедневный» (на срабатывании +1 день), структура — задел. Срабатывание
  — чайм+голос на `reminder_volume`. Произношение дат — `speech.say_date_natural`/`say_when` (склонения:
  «завтра», «во вторник», «пятнадцатого июня»). ~32 параметра+пака — секция `reminders`. doctor
  `check_alarms` расширен (даты/напоминания/задачи). `jarvis doctor --quick` зелёный (✗0).
- **Этап 14 (распознавание: точность/шум/скорость/push-to-talk) — ✅ КОД ГОТОВ (живая проверка за
  пользователем).** ГЛАВНОЕ — **push-to-talk**: зажатие правого Ctrl (кнопка в settings.yaml) → STT
  слушает команду БЕЗ wake-word, пока держишь; отпустил → распознаёт (надёжный путь в шуме И скоростной
  — без 0.8с VAD-паузы). Реализация: поток-читатель `/dev/input/eventX` клавиатур (raw struct, как
  `activity_monitor`; нужна группа input), копит аудио → ASR → `jarvis/input`. **Ducking музыки** на
  время прослушивания по PTT (reuse `AudioEnv.duck/restore`). **Точность** (диагностика на машине):
  матчер ИДЕАЛЕН на 19 формулировках (0.93–1.0, правила) — НЕ узкое место; корень повторов — захват
  (тихий онсет → адаптивный порог ТЗ-1; музыка/шум → ducking+PTT). Пополнены wake-word варианты
  (чарвис/чарвиз/джарви). **Скорость** (профиль): ASR-декод 62мс при `num_threads=1` — БЫСТРЕЕ, чем 2
  (111мс)/3 (142мс): больше потоков ВРЕДИТ на крошечной int8-модели (НЕ менять); матчер <30мс — цепочка
  уже оптимальна; основной выигрыш — PTT мгновенно. **Шумоподавление**: RNNoise недоступен; echo-cancel
  есть, но sounddevice его не читает (PortAudio видит только «default») → авто-интеграция отброшена,
  echo-cancel — ручная опция (ALSA). Честный предел: 100% в громком шуме недостижимо; PTT — обход.
  doctor `check_push_to_talk`. `jarvis doctor --quick` зелёный (✗0).
- **Этап 15 (цепочки: продолжения без wake-word, комбо, история) — ✅ КОД ГОТОВ (живая проверка за
  пользователем).** Новый `jarvis/chains.py` + правки STT/core/matcher/scheduler/activity_monitor.
  (1) ПРОДОЛЖЕНИЯ: после команды активна её ВЕТКА (карта `ветки:` в commands.yaml — звук/музыка/
  яркость/браузер/сеть/ночной из существующих команд); пока ветка активна, её продолжения принимаются
  БЕЗ «Джарвис» (СТРОГО: только продолжения; новая полная команда требует wake/PTT). STT публикует
  `wake:false` + всю фразу; core матчит строго ПРАВИЛАМИ в подмножестве ветки (`match(allowed_tags,
  use_embeddings=False)`), ветку при продолжении НЕ меняет. Нет ограничения по времени. (2) КОМБО:
  «выключи блютуз и вай-фай» → split по «и/потом/затем» → match каждого (с подстановкой глагола 1-й
  части для эллипсиса «…и вай-фай») → ≥2 → выполнить по порядку + последовательные подтверждения;
  частично — пак `combo_partial`. (3) ИСТОРИЯ (in-memory): «повтори последнее» → пере-execute;
  «отмени» → обратный тег из карты `обратимость:` (volume/brightness/wifi/bluetooth/mute/night),
  необратимо/нечего → пак. doctor `check_chains`. `jarvis doctor --quick` зелёный (✗0). Сервисов 6.
- **Этап 16 (режим тишины + дубль речи в уведомления + элегантные сбои с кнопкой в kitty) — ✅ КОД
  ГОТОВ (живая проверка за пользователем).** Новые `jarvis/silence.py` (гейт «без звука»/«включи звук»
  + состояние-файл `logs/silence_state.json`, переживает рестарт) и `jarvis/notify.py` (уведомления
  через **gdbus**, т.к. notify-send нет; кнопка «Открыть логи» через actions + сигнал ActionInvoked).
  (1) РЕЖИМ ТИШИНЫ: команда ловится из эфира ВСЕГДА (как стоп-фраза, ТОЧНОЕ совпадение — «включи звук»
  ≈ «выключи звук» difflib бы путал); core toggle (ON-ack→уведомление, OFF-ack→голос); TTS в тишине
  обычные фразы шлёт в уведомление, КРИТИЧЕСКОЕ (срабатывания — помечены `min_volume`) звучит всегда.
  (2) ПРОВЕРКА ЗВУКА перед фразой (`sysinfo.read_volume`, кэш): громкость 0/muted → пак `audio_zero`;
  сбой/устройство недоступно → пак `audio_fail` + причина (РАЗНЫЕ пометки) → дубль фразы в уведомление.
  (3) СБОИ: `JarvisModule.notify_failure` (человеческий текст без трейсов + кнопка→лог модуля), хук в
  `request_restart` и при немоте TTS. Слушатель ActionInvoked (`gdbus monitor`) — в **os_agent** (запуск
  процессов = «Руки»; whitelist юнитов) → `kitty -e journalctl --user -u <unit>`. «без звука»/«беззвучный
  режим» перенесены из volume_mute, «включи звук» — из volume_unmute (в silence). doctor
  `check_notifications`. `jarvis doctor --quick` зелёный (✗0). Сервисов 6 (без новых).
- **Возможное будущее (необязательно):** системное (перезагрузка, jarvis live, среды) — отдельное ТЗ.
  HUD — киношный визуал поверх готового пульта (веб-стек в прозрачном окне на Wayland, чисто
  визуальный). Только после устойчивой работы.

Каждый этап — отдельный заход, проверка на железе перед следующим.

---

## РАБОЧИЕ ПРИНЦИПЫ

- **Plan mode для всего нетривиального.** План → подтверждение → код.
- **Один заход = одно связное изменение.** Не монолит. Узкая чёткая задача лучше расплывчатого куска.
- **СВЕРЯЙ API НА МАШИНЕ, не по памяти.** Версии меняются. Спасало многократно (метод
  Piper; имена входов ONNX и mean-pooling rubert-tiny2; параметр `num_threads` у sherpa).
  Трогаешь внешнюю либу/модель — сперва проверь сигнатуру на установленной версии.
- **Не верить «готово» без проверки.** `jarvis doctor` зелёный (FAIL=0; WARN ок).
  Отчёт расходился с реальностью — проверять вживую.
- **Сервисы не перезапускать самому** — это проверка пользователя.
- **Обязательно `git commit` + `git push`** в конце захода. Браузерный CC раньше пушил
  в свои ветки и работа «терялась» — проверять `git branch -a`, пушить явно.
- Команды под KDE/Wayland проверять реально (бинарь в `$PATH` ≠ рабочий эффект).

---

## КОМАНДЫ ПРОЕКТА

- `jarvis start` — поднять/перезапустить сервисы (делает restart).
- `jarvis status` — статус юнитов.
- `jarvis doctor` (`--quick` — без синтеза/цепочки) — диагностика: импорты, версии,
  пути, железо, MQTT round-trip, модели STT/TTS, эмбеддер (грузится + осмыслен),
  матчер (команды распознаются по синонимам), сервисы, сквозная цепочка.
- `jarvis models --download` — скачать модели по `models.yaml` (вкл. rubert-tiny2-onnx).
- `pip install -e .` (в venv) — переустановить пакет после правок зависимостей.

---

## ИЗВЕСТНЫЕ ГРАБЛИ

- **Пути в `settings.yaml` — резолв ТОЛЬКО от корня проекта (регрессия Этапа 7, не повторить):**
  пути там можно писать относительными (`models_dir: "models"`), НО под systemd рабочая
  директория = `$HOME` (юниты без `WorkingDirectory`) → относительный путь не находится
  (`FileNotFoundError` на моделях/commands.yaml): STT падает насмерть, а core/os-agent/tts
  «зомби-живые» — `active`, но с пустой картой команд / без голоса (лениво глотают ошибку).
  Фикс закреплён: `config.py` приводит ВСЕ пути к абсолютным через `_get_path` (относительный →
  от `BASE_DIR`); юниты получают `WorkingDirectory={BASE_DIR}` (страховка); `doctor` FAIL'ит
  относительный путь (раньше проверял лишь `exists()` → из корня проекта ложно зеленел и
  ПРОПУСТИЛ регрессию). Новые пути-ресурсы добавляй ТОЛЬКО через `_get_path` (секция `models`
  в config — это исключительно пути; прочие параметры — в своих секциях через `_get`).
- **Переезд на TUXEDO OS (Ubuntu-база):** бинарии команд отличаются от Kali. Браузер —
  `thorium-browser` (НЕ firefox-esr; запущен из `/opt/chromium.org/thorium`, бинарь-обёртка
  в `/usr/bin`). `mosquitto` (брокер) и `brightnessctl` (яркость) на новой системе ставятся
  через `sudo apt install`. konsole/spectacle (`-b -f -n` работают)/dolphin/wpctl/nmcli/
  bluetoothctl/loginctl — на месте. Терминал — **kitty**. Приложения: kcalc/kate/gwenview/
  okular/plasma-systemmonitor/systemsettings. Рабочие столы — `qdbus6 org.kde.KWin` (Wayland).
- **Команды захода 5 (вширь):** сайты — `thorium-browser <url>`. Папки — `dolphin` по русским
  XDG-путям (`/home/tux/Загрузки` и т.д.), корзина `trash:/`. Носители — `/media/tux/<МЕТКА>`
  (TOSHIBA, Ventoy) + поле `требует_путь` в commands.yaml (не подключён → ответ в характере, без
  запуска). Ночной режим — `qdbus6 org.kde.kglobalaccel /component/kwin invokeShortcut "Toggle
  Night Color"` (это TOGGLE; прямого on/off в NightLight DBus нет — только `stopPreview`/`Ping`).
  VPN **Amnezia — только запуск GUI** (`/usr/local/sbin/AmneziaVPN`; CLI awg/wg-quick нет, конфиги
  root → вкл/выкл голосом невозможно). **gparted — только запуск** (polkit спросит пароль).
- **Вкладки браузера = ydotool (Wayland):** клавиши шлются только через `ydotool` (`ydotool key
  29:1 20:1 20:0 29:0` = Ctrl+T) — нужен `sudo apt install ydotool` + udev-правило на `/dev/uinput`
  (группа `input`, MODE 0660) + `usermod -aG input` + демон `ydotoold` + **ПЕРЕЛОГИН** (группа
  применяется только после него). Без этого `tab_*` дают WARN (бинаря нет). xdotool/wmctrl на
  Wayland НЕ работают.
- **Произношение чисел (`speech.py`):** время/дата/проценты в info-ответах — словами («четырнадцать
  часов тридцать минут», «седьмое июня…», «сорок пять процентов»), иначе Piper читал криво. Все
  числа в озвучке идут через `speech.py`; падежи в `core.py` подобраны под него (винит./именит.).
- **Адаптивная громкость (`audio_env.py`):** меряем РЕАЛЬНЫЕ RMS сигнала, НЕ позиции регуляторов.
  Уровень воспроизведения — `pw-cat --record --target <ID>` monitor-source (по ИМЕНИ ЗАВИСАЕТ —
  только по ID, находить через `pactl list short sources`). Внешний шум = mic − k·monitor (вычесть
  свой звук/голос → нет самоподхвата). Ducking музыки — `pactl set-sink-input-volume` (своё `pw-cat`
  исключаем по имени бинаря). Громкость речи пользователя (`user_level`) идёт STT→core→TTS опц. полем
  в input/say. Пороги/калибровка — в config (RMS-единицы). Замерщики ~2% ядра. Сбой → фикс. громкость.
- **Шумоподавление STT (опц.):** RNNoise в apt НЕТ; есть `module-echo-cancel` (WebRTC AEC+denoise).
  STT слушает обработанный source через `JARVIS_STT_SOURCE` (системный default НЕ меняем). VAD-пороги
  в шуме НЕ трогаем — шумоустойчивость через denoise+ducking+усиление голоса.
- **НЕМОТА на TUXEDO (важно, не повторить):** sounddevice/PortAudio видит ТОЛЬКО сырые
  ALSA-выходы (HDMI), не аналог/PipeWire → `sd.play` слал звук в HDMI на 22050 Гц →
  `paInvalidSampleRate`, исключение глоталось как WARNING. Фикс: TTS играет через **`pw-cat`**
  (PipeWire, default sink, авто-ресемплинг). НЕ возвращать sounddevice в `tts.py`; `pw-cat`
  обязателен (есть чек в doctor). Захват STT при этом остаётся на sounddevice (он работает).
- **swap = 0 на TUXEDO** (на Kali был). На 8 ГБ без swap выше риск OOM-kill голосового
  конвейера под нагрузкой. Рекомендация — включить zram-swap (`sudo apt install zram-tools`).
  Жёсткий `MemoryMax` в юнитах намеренно НЕ ставим (лимит → OOM убил бы пульт).
- `commands.yaml`: команды с `ждать_вывод: true` (сейчас `network_status`) пишут
  `cmd_<тег>_<время>.log` в `logs/` и НЕ ротируются — периодически чистить при активном use.
- OS-агент дожинает fire-and-forget процессы (`_reap`), иначе короткие команды (wpctl/
  brightnessctl) копились бы зомби `<defunct>` — родитель обязан делать `wait()`.
- `telegram-desktop` не установлен → команда `telegram` даёт WARN (не блокер). Так же
  `playerctl` (медиа) и `wl-clipboard` (буфер) ставятся `sudo apt install` — без них команды
  `media_*`/`clipboard_clear` дают WARN. Уровни громкости/яркости семантически близки —
  направление задают ПРАВИЛА (синонимы), эмбеддинги по margin переспросят (безопасно).
- Анти-эхо: при низком VAD-пороге микрофон ловит голос Джарвиса из колонок. Защита:
  STT молчит при `state=speaking` + хвост `SPEAKING_TAIL=1.0` от конца воспроизведения +
  сброс VAD-буфера при возобновлении + подавление по содержанию (difflib). Требует
  проверки вживую на быстрых фразах встык.
- **Эмбеддинги НЕ различают антонимы** (вкл/выкл, громче/тише — косинус ~0.97/0.80).
  Направление ОБЯЗАН задавать слой правил конкретными синонимами; иначе матчер по
  margin-защите переспросит, а не угадает (это правильное безопасное поведение).
- Эмбеддер rubert-tiny2 в RAM ~165–190 МБ — поэтому ленивый; arena/mem-pattern в
  onnxruntime RSS не уменьшают (сверено). Если эмбеддер не нужен — правила работают без него.
- `rubert-tiny2` официально только в PyTorch; используем готовый ONNX-экспорт
  `Vuy/rubert-tiny2-onnx` (конвертация своими силами требовала бы torch — нельзя).
- **Idle на KDE6/Wayland НЕЛЬЗЯ опрашивать (Этап 9, не повторить ошибку ТЗ):** D-Bus
  `org.freedesktop.ScreenSaver.GetSessionIdleTime` → `NotSupported` (KWin-заглушка), logind
  `IdleHint` не обновляется, PowerDevil idle по D-Bus не отдаёт. На Wayland idle принципиально
  СОБЫТИЙНЫЙ. swayidle/`ext-idle-notify` `get_idle_notification` УВАЖАЮТ idle-инхибиторы →
  полноэкранное видео считалось бы активностью (нарушение «фильм=простой»). Поэтому
  `activity_monitor` читает `/dev/input/event*` напрямую (чисто-входной простой). ТРЕБУЕТ
  группу `input` (`sudo usermod -aG input tux` + ПЕРЕЛОГИН — та же, что нужна ydotool); без
  неё сервис «спит» (молчит, не спамит), doctor WARN'ит фикс.
- **Фильтр устройств ввода — биты EV (Этап 9):** в `B: EV=<hex>` из `/proc/bus/input/devices`
  бит N взведён = поддерживается тип события N. EV_KEY=тип1=бит `0x02`, EV_REL=тип2=`0x04`,
  EV_ABS=тип3=`0x08`, EV_REP=тип20=`0x100000`. Бит `0x01` = EV_SYN (у ВСЕХ — не показателен!).
  Берём указатели (EV_REL|EV_ABS) и клавиатуры (EV_KEY+EV_REP) → ровно мышь/тачпад/клавиатура,
  без датчиков/Lid/Power/Video Bus (иначе iio-sensor-proxy даёт ложную «активность»). Маски
  СВЕРЕНЫ на железе — спутать «номер типа» с «битом» легко (так и был пойман баг).
- **Затемнение экрана (Этап 9):** `brightnessctl` (raw get/max/set через logind активной
  сессии, без OSD-всплывашек, в отличие от KDE-DBus `org.kde.ScreenBrightness`). Запоминаем
  исходную raw ДО затемнения, плавная рампа, возврат — ТОЛЬКО при засчитанном перерыве/возврате
  из простоя/стоп-фразе. Рассинхрон с голосовыми `brightness_*`: на `jarvis/execute` с тегом
  `brightness*` монитор отменяет «долг» восстановления (не воюем с пользователем). Рестарт сервиса
  в затемнённом виде лечится state-файлом `logs/break_dim_state.json` (на старте вернёт яркость).
- **Стоп-фразы перерыва vs команды (Этап 9):** `jarvis/breaks.py::is_stop_phrase` сверяет ВЕСЬ
  нормализованный текст (ё→е, без пунктуации) с каждой стоп-фразой по difflib на ПОЛНОЕ
  совпадение, порог ВЫСОКИЙ (0.85) — иначе стоп-фраза тихо проглотила бы похожую команду.
  Проверено: 0 коллизий на 531 синониме commands.yaml. core молчит на стоп-фразу (не «не
  разобрал»), ответ даёт `activity_monitor` (только в контексте напоминания). Текст в input уже
  БЕЗ wake-word (STT снимает «джарвис»), стоп-фразы пишем как остаток: «не сейчас», «я работаю».
- **Тон голоса (Этап 10): Piper НЕ умеет pitch** (сверено: `SynthesisConfig` — только `length_scale`/
  `volume`/`noise_*`). Понижение тона — подменой частоты воспроизведения: `pw-cat --rate =
  частота_модели·pitch` (ниже частота → ниже тон И медленнее), а темп компенсируем `length_scale =
  user_length·pitch` → итог: net-темп = `voice.length_scale`, net-тон = `voice.pitch` (НЕЗАВИСИМЫ).
  Не формант-сохраняющий — чисто только для МАЛОГО сдвига (≈0.88–1.0); сильный «поплывёт». Доп.
  зависимостей/CPU нет (pw-cat и так ресэмплит к частоте устройства). Формант-сохраняющий pitch-shift
  потребовал бы phase vocoder (тяжело на N100) — намеренно НЕ делаем.
- **Общий механизм фраз (Этап 10, ФУНДАМЕНТ):** `jarvis/phrases.py::pick(key, pak)` — выбор без
  повторов В ПРЕДЕЛАХ ЦИКЛА (исчерпали пак → обнуление; новый цикл не начинаем с только что сказанной),
  состояние по ключу пака, потокобезопасно, в памяти процесса. Ключи: `startup.ok/warn`, `confirm:<тег>`,
  `confirm.generic`, `break.offer/praise/reply`. Прежний `breaks.pick_phrase` (избегал лишь предыдущей)
  УДАЛЁН — все паки идут через `phrases.pick`. ЛЮБОЙ новый пак фраз — только через него (единообразно).
- **Стартовое объявление (Этап 10):** раньше старт МОЛЧАЛ; «каша при старте» = тестовые фразы
  `jarvis doctor` («Проверка связи» + переспрос на нераспознанное). Теперь `cli.py::cmd_start` после
  рестарта ждёт осадки (~7с: модели/Piper) и по `systemctl is-active`+`SubState=running` ВСЕХ юнитов
  выбирает пак `startup.success_phrases`/`problem_phrases` и публикует в `jarvis/say` разовым клиентом.
  Type=simple → 'active' ≠ «готов»: даём осесть + два замера (крэш-цикл не держится 'running' на обоих).
  Сбой объявления в try-except (не влияет на код возврата). Выключатель `startup.announce`.
- **Адаптивный порог VAD (Этап 10): база 0.3 СВЯЩЕННА — смещаем только ВНИЗ в тишине.** Живой правки
  порога в sherpa-onnx нет (баком при конструировании) → `stt._build_vad(threshold)` ПЕРЕСОБИРАЕТ
  детектор (silero крошечный, дёшево), но ТОЛЬКО при смене полосы тишина/шум, с гистерезисом вокруг
  `QUIET_THRESHOLD` и не чаще раз в 10с — В ТОМ ЖЕ потоке захвата (нет гонки с `accept_waveform`).
  Шумовой пол — по СВОЕМУ потоку STT (без второго аудиострима): асимметричная EMA, обновляется лишь
  когда `vad.is_speech_detected()`==False (речь не считаем фоном). В тишине порог 0.3→0.22 (config:
  `vad_quiet_offset`/`vad_quiet_floor`), в шуме строго назад к 0.3. Базовое поведение в норме/шуме НЕ
  меняется. **Причина повторов команд (по логам, не гадание):** тихое НАЧАЛО фразы (wake-word — самая
  тихая часть онсета) не открывало VAD → STT слышал команду без wake-word → `_match_wake_word`→None →
  команда молча отбрасывалась (в логах «ой файлы» без публикации; удачные ретраи при user_level
  0.017–0.025). Адаптивный порог ловит тихий онсет с первого раза. Wake-word fuzzy-порог НЕ трогаем.
- **Будильники: распознавание — слой ПРАВИЛ, не эмбеддинги (Этап 11).** Действия set/move/cancel —
  АНТОНИМЫ (поставь/отмени/удали), эмбеддинги их путают (как вкл/выкл). Поэтому `alarms.parse_command`
  — ключевые слова + difflib + `parse_time`, БЕЗ эмбеддера (надёжно, легко, антонимо-безопасно).
  Команды НЕ в commands.yaml (там OS-команды с полем `команда`; доктор требует непустой список).
  Гейт `alarms.is_alarm_command` ОБЩИЙ: core молчит, scheduler обрабатывает (паттерн `is_stop_phrase`).
  Гейт ловит «будильник»/«разбуди»/«подъем»/«утренн» (утренний можно без слова «будильник»: «перенеси
  утренний на 6»).
- **Парсер времени (`alarms.parse_time`, Этап 11):** период суток (утра/дня/вечера/ночи) применяется
  И к особым формам — «полвосьмого ВЕЧЕРА»=20:30 (период считаем ДО возврата из ветки). «семь тридцать»
  ≠ составное (единица перед десятком) → 7:30, а «двадцать один» = составное 21 (десяток+единица в
  `_read_number`). Падежи «половина/половину/половине» (`половин\w*`). Регекс-альтернация: `половин\w*`
  ПЕРЕД `пол` (иначе «пол» съел бы префикс «половину»). 28/28 тестов на формы — не упрощать без тестов.
- **Open-Meteo (погода, Этап 11): ОПЦИОНАЛЬНА, best-effort.** stdlib `urllib` + таймаут (config), любой
  сбой/нет сети → None (будильник звонит БЕЗ погоды, не падает/не виснет). Геокодинг принимает кириллицу
  напрямую («Москва»); из «Город, Страна» берём часть до запятой. Координаты КЭШируются в
  `logs/geocache.json` (1 запрос на регион). weather_code (WMO) → русский «характер дня» (таблица в
  `weather.py`). Прямой HTTPS, БЕЗ прокси/VPN (пользователь — пентестер). Погода кэшируется на день.
- **Громкость будильника = поле `min_volume` в `jarvis/say` (Этап 11):** TTS берёт `max(min_volume,
  адаптивная)` → будит ГРОМКО даже в тишине (обход «тихо→тихо»). Нарастание утра = несколько коротких
  реплик (`wake_prelude`) с растущим `min_volume`, затем богатая фраза на `wake_volume`. Поле опционально,
  обычные say без него — как раньше. НЕ путать с `user_level` (адаптив для обычных ответов).
- **schedule.yaml (Этап 11): код ПЕРЕЗАПИСЫВАЕТ при голосовых правках** (`yaml.safe_dump` + генерируемая
  шапка) → ручные комментарии пользователя НЕ сохранятся (значения — да); это задокументировано в шапке
  файла. Запись АТОМАРНАЯ (tmp + `os.replace`). Путь — `config.SCHEDULE_FILE` через `_get_path` (абсолютный
  для systemd). Runtime-состояние (последнее срабатывание, анти-двойное) — отдельно в
  `logs/scheduler_state.json` (не засоряет расписание). Утренний — singleton id=`morning`; обычные id —
  по метке/времени. Тик `datetime.now()` (локальное wall-clock), окно grace для пропущенных (сон).
- **Новый сервис требует `pip install -e .` (Этап 11):** entry point `jarvis-scheduler` в pyproject.toml
  → без reinstall ExecStart-бинаря в venv нет, юнит падает, doctor WARN'ит «юнит не установлен». После
  reinstall + `jarvis start` юнит генерируется из `services_map.SERVICES` автоматически.
- **Таймеры/секундомеры (Этап 12): термины и хранение.** СЕКУНДОМЕР считает ВВЕРХ (elapsed = now −
  `старт`), ТАЙМЕР — обратный отсчёт (срабатывает в `окончание`). Оба — в `schedule.yaml` (ключи
  `таймеры`/`секундомеры`; `alarms.read/write_schedule` владеет тремя списками). Переживают перезапуск
  по ISO-времени старта/окончания (не по «оставшимся секундам» — иначе сон сбил бы). Истёкший за время
  простоя таймер срабатывает при первом тике (флаг `сработал` — анти-повтор). «Удали все» РАЗДЕЛЬНО по
  типам (таймеры / секундомеры / обычные будильники); НЕ путать. Распознавание — `jarvis/timers.py`,
  слой правил (антонимы set/stop/reset эмбеддинги путают), НЕ в commands.yaml.
- **Парсер длительности (`timers.parse_duration`, Этап 12):** «голая» единица без числа = 1 («на час»=
  3600, «на минуту»=60); остаточное число без единицы → минуты («час двадцать»=1ч20м); «полтора/полторы»
  +единица=×1.5, «полчаса»=1800, «полминуты»=30; составное «двадцать пять» через `_read_num`. Метку
  отрезаем ДО парсинга длительности (`_duration_part`) — иначе числа из метки попали бы в длительность.
  20/20 тестов — не упрощать без тестов.
- **Мировое время (`worldtime.py`, Этап 12): город в ПРЕДЛОЖНОМ падеже, а geocoding ищет ИМЕНИТЕЛЬНЫЙ.**
  Сверено: «париже» НЕ находится, «киеве»→ошибочно «Киёвец». Поэтому генерируем кандидаты-формы
  (париже→париж/парижа, москве→москва, твери→тверь) и пробуем, пока geocoding не найдёт; результат
  кэшируется (`logs/worldtime_cache.json`). Дефис СОХРАНЯЕМ (Нью-Йорк, Санкт-Петербург — отдельная
  нормализация, НЕ `alarms._norm`). Часовой пояс — из ответа geocoding, время/летнее — `zoneinfo`
  (stdlib), разница — от РЕГИОНА (`местоположение/регион`). Имя города в ответе — именительный из
  geocoding, через «в городе X» (склонять pymorphy на N100 не тянем). Многословные ПАДЕЖНЫЕ домашние
  города («нижнем новгороде») могут не разрешиться → внятный отказ (не падение). detect_city — ПЕРЕД
  локальным временем в core (иначе «сколько времени в …» поймал бы локальный _TIME_KEYS).
- **Чайм таймера (Этап 12):** короткий синус-тон генерится в TTS (`_chime_pcm`, numpy) и пишется в тот
  же `pw-cat` ПЕРЕД синтезом фразы — поле `chime` в `jarvis/say` (как `min_volume`). Без звуковых
  файлов/зависимостей. Громкость задаёт `timer_volume` (как у будильника, обход «тихо→тихо»). Параметр
  `timers.timer_chime` (вкл/выкл). Срабатывание уважает будущий режим тишины через те же поля say.
- **Озвучка длительности (`speech.say_duration`, Этап 12):** склонения час(м)/минута(ж)/секунда(ж),
  нулевые компоненты ОПУСКАЮТСЯ («пять минут», «один час три минуты», «двадцать одна секунда»), 0 →
  «ноль секунд». Время будильника — `say_clock` (12-часовой+период), длительность — `say_duration`
  (часы+минуты+секунды) — НЕ путать.
- **Гейт напоминаний — ДВЕ основы (Этап 13):** «напомн-и/-ить» (сов.вид) и «напомин-ай/-ание/-ать»
  (несов.вид, для повтора «напоминай каждый день») — РАЗНЫЕ основы; «напомн» НЕ ловит «напоминай», и
  наоборот. `is_reminder_command` проверяет ОБЕ («напомн» и «напомин»). Поймано тестом.
- **parse_when: день vs время (Этап 13):** «15 июня» — это ДЕНЬ, не 15:00. Время берём ТОЛЬКО при явном
  маркере (`:`/час/утра-вечера/полвосьмого/без…/четверть) ИЛИ «в N» (с negative-lookahead на месяц/
  «число»/«-го»). Иначе точное=False (без времени). Парсер дат (`reminders.parse_date`) — относит.+
  абсолют.+дни недели+порядковый день словом («пятнадцатого» через реверс `speech._ORD_GEN_*`); 16/16.
- **Диалог дозапроса (вар.3, Этап 13): кросс-процессный флаг.** «напомни» без деталей → scheduler
  спрашивает о чём/когда и держит частичку в ПАМЯТИ; общий файл `logs/dialog_state.json` (active_until)
  сигналит core МОЛЧАТЬ на любой ввод (ответ-продолжение «позвонить маме» не содержит «напомни»).
  Таймаут (config `dialog_timeout_seconds` ~45с) + «отмена» → сброс; рестарт mid-диалога → флаг истечёт.
  В _on_input scheduler диалог проверяется ПЕРВЫМ (до гейтов). Ответ может содержать и текст, и время.
- **Напоминания: вычисление срабатывания (Этап 13).** Режим1 (точное): дата@время; если время сегодня
  прошло и дата НЕ задана → +1 день. Режим2 без времени: СЕГОДНЯ → now+random(1.5–3ч, точное=False,
  пак `set_random`/`fire_random`); БУДУЩАЯ дата → random в дневном окне (config 10–18, не ночью).
  Повтор «ежедневный» → на тике срабатывание +1 день в цикле до будущего (не спамит за пропущенные дни).
  Задача с `дедлайн` срабатывает как напоминание (флаг `сработал`). Привязка move/cancel/done — по
  метке ИЛИ ВХОЖДЕНИЮ текста (difflib≥0.6). «Удалить все» раздельно: напоминания / задачи / (будильники/
  таймеры/секундомеры из прошлых ТЗ не трогаются).
- **Push-to-talk (Этап 14): чтение /dev/input напрямую (Wayland).** STT-поток читает сырые
  `input_event` (struct `llHHi`=24 байта) с клавиатур (Handlers `kbd` из `/proc/bus/input/devices`),
  фильтр `EV_KEY`+код кнопки: value 1=нажата, 0=отпущена, 2=автоповтор (игнор). Зажата → копим аудио
  (минуя wake-word/VAD), отпущена → ASR на буфере → `jarvis/input` БЕЗ wake-word. Флаг `_ptt` пишет
  поток-читатель, буфер ведёт ТОЛЬКО поток захвата (нет гонки, GIL-атомарный bool). Нужна группа
  `input` (та же, что ydotool/монитор); нет — кнопка молчит, wake-word работает, doctor WARN. Кнопка
  по имени→код в `config._PTT_KEYCODES` (правый ctrl=97). Работает ПАРАЛЛЕЛЬНО с wake-word; PTT ещё и
  быстрее (нет 0.8с VAD-паузы — обработка на отпускании).
- **Шумоподавление НЕ авто (Этап 14): sounddevice не читает denoised-источник.** Сверено: PortAudio
  собран только с ALSA, видит лишь «default»=железный микрофон; ни по имени, ни `PULSE_SOURCE`, ни
  `set-default-source` denoised-источник не цепляется (не уходит в RUNNING); `pw-cat --record` его
  отдавал лишь обрывки. RNNoise/LADSPA на машине НЕТ; `module-echo-cancel` (WebRTC) грузится, но без
  глобальной смены default sink не убирает музыку (нужен референс-выход) → авто-интеграция отброшена
  (хрупко/интрузивно). Шум давят: ducking музыки при PTT + адаптивный порог VAD (ТЗ-1) + сам PTT.
  echo-cancel — ручная опция через ALSA (для желающих). ЧЕСТНО: 100% в громком шуме недостижимо.
- **Потоки ASR=1 — ОПТИМАЛЬНО (Этап 14, не повышать).** Профиль: декод 62мс@1поток < 111мс@2 < 142мс@3
  — на крошечной int8-zipformer накладные расходы потоков перевешивают параллелизм. `stt_num_threads=1`
  держим (и для VAD, и для распознавателя). Эмбеддер НЕ греем заранее (ленивость = экономия ~165МБ RAM
  на 8ГБ; первый промах +23мс — редко и приемлемо). Цепочка распознавания уже тугая; рычаг
  ВОСПРИНИМАЕМОЙ задержки — VAD `vad_min_silence` 0.8с (закреплён, настраиваем; PTT его обходит).
- **Флаг `wake` в jarvis/input (Этап 15) — все подписчики ОБЯЗАНЫ его учитывать.** STT теперь публикует
  и фразы БЕЗ wake-word (`wake:false` + вся фраза) для продолжений. Подписчики `jarvis/input`: **core**
  (wake=false → только продолжение активной ветки), **scheduler** (wake=false вне диалога → ИГНОР, иначе
  «поставь будильник» из ТВ ставило бы будильники; ответ диалога без wake — обрабатывается ДО гейта),
  **activity_monitor** (активностью считает ТОЛЬКО wake=true — иначе речь из ФИЛЬМА сбрасывала бы простой,
  ломая ТЗ-9 «фильм=простой»; стоп-фразу обрабатывает всегда). Нет поля = true (обратная совместимость).
  PTT публикует wake:true (намеренная команда). Новый подписчик jarvis/input — добавь проверку `wake`.
- **Продолжения = СТРОГО правилами в подмножестве ветки (Этап 15).** `matcher.match(allowed_tags=ветка,
  use_embeddings=False)`: в МАЛОМ наборе эмбеддинги «перематчивают» неродственное («открой браузер»→
  music_yandex 0.71) — поэтому продолжение требует ТОЧНОГО синонима (правила), эмбеддинги выключены.
  Продолжение НЕ меняет активную ветку (контекст: музыка→«тише» остаётся музыкой; иначе volume_down имеет
  «домашнюю» ветку звук и контекст бы терялся). Ветку задаёт только полная команда (wake) или комбо.
- **Служебные top-level ключи commands.yaml (Этап 15): `ветки`, `обратимость` — НЕ команды.**
  `matcher._RESERVED_KEYS` пропускает их в `_build_index`; `doctor.check_commands_yaml`/`check_matcher`
  тоже их фильтруют (иначе FAIL «нет синонимов»/«команда настроена неверно»). Новый служебный ключ →
  добавь в `_RESERVED_KEYS`. Ветки/обратимость ссылаются на СУЩЕСТВУЮЩИЕ теги (doctor `check_chains` это
  проверяет). Продолжения переиспользуют `подтверждение`-паки своих команд — отдельных фраз им не нужно.
- **Комбо-split (Этап 15): эллипсис глагола.** «выключи блютуз и вай-фай» → ['выключи блютуз','вай фай'];
  2-я часть без глагола → core подставляет глагол 1-й части («выключи вай фай»→wifi_off). Split по
  отдельным словам «и/потом/затем/плюс/а также/дальше»; <2 распознанных действия → НЕ комбо (откат к
  одиночному матчу, «и» могло быть частью одной команды). os_agent НЕ менялся: комбо = N execute по
  очереди (QoS1 хранит порядок). Некоторые примеры ТЗ (тёплый экран «теплее/холоднее», «закрой браузер»)
  на Wayland как OS-команды не реализуемы → продолжения строим из фактически работающих команд.
- **Уведомления — через gdbus, НЕ notify-send (Этап 16): на машине notify-send НЕТ.** `jarvis/notify.py`
  шлёт `gdbus call … Notify` (subprocess, без зависимостей); сервер `org.freedesktop.Notifications`
  поддерживает `actions` (кнопки, сверено GetCapabilities). Кнопка «Открыть логи» = действие
  `["logs:<unit>", label]`; клик → сигнал `ActionInvoked(u id, 'logs:<unit>')`. Слушатель — `gdbus
  monitor` в **os_agent** (он жив всегда и запускает процессы): парс `_ACTION_RE`, whitelist
  `_ALLOWED_UNITS` (только наши юниты — защита от инъекций) → `kitty -e bash -lc "journalctl --user -u
  <unit>.service -n 300 -f"`. notify() возвращает id (replace_id заменяет «речевые» уведомления, чтобы
  не копились). Любой сбой gdbus → 0 (тихо). Если упал сам os_agent — кнопка его лога не сработает.
- **Режим тишины — ТОЧНОЕ совпадение, не difflib (Этап 16).** `silence.is_silence_on/off` сверяет ВЕСЬ
  нормализованный текст НА РАВЕНСТВО фразам: «включи звук» (выход) и «выключи звук» (mute) отличаются на
  букву → difflib (0.96) их путал бы. Состояние — `logs/silence_state.json` (атомарно, переживает
  рестарт), читают core (отвечает) и TTS (голос/уведомление). Команда ловится в `core.on_input` ПЕРВОЙ
  (до wake-роутинга), и с wake, и без (как стоп-фраза). ⚠ «без звука»/«беззвучный режим» УБРАНЫ из
  синонимов `volume_mute`, «включи звук» — из `volume_unmute` (теперь это команды тишины; mute/unmute
  остались на «выключи/убери/заглуши» и «верни/восстанови/вруби звук»).
- **Критично = звучит даже в тишине (Этап 16): сигнал `min_volume` (или явный `critical`).** TTS:
  `critical = critical-флаг or min_volume is not None`. Срабатывания будильников/таймеров/напоминаний
  ставят `min_volume` (а подтверждения «поставлено» — нет) → они и есть «критическое»: озвучиваются в
  тишине и в уведомления НЕ дублируются. Обычные фразы в тишине → только уведомление (replace_id, чтобы
  не копились).
- **Проверка звука перед фразой: «ноль» vs «сбой» (Этап 16) — через `sysinfo.read_volume` (wpctl).**
  `{выключен:true}` или `громкость_процент<=0` → 'zero' (пользователь убавил — пак `audio_zero`);
  `{ошибка}` → 'fail' (устройство/PipeWire недоступны — пак `audio_fail` + причина); сбой `pw-cat` при
  игре → тоже 'fail' («поток не открылся»). Результат кэшируется `AUDIO_CHECK_TTL` (~1.5с) — не дёргать
  wpctl на каждую фразу. В обоих сбойных случаях фраза ДУБЛИРУЕТСЯ в уведомление с пометкой ПРИЧИНЫ
  (две РАЗНЫЕ). Критическое при сломанном звуке тоже дублируется (fallback, иначе не услышит).
