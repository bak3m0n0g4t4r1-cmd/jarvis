# Зависимости Фазы 1. Дублируют dependencies из pyproject.toml для удобства.
# Установка проекта целиком: pip install -e .
# ВНИМАНИЕ: точные версии sherpa-onnx и piper-tts сверять с тем, что реально
# встаёт на Kali N100 (pip show <пакет>, --help, docstrings).

paho-mqtt>=2.1,<3
pydantic>=2.5,<3
pyyaml>=6,<7
numpy>=1.26
sounddevice>=0.4.6
ollama>=0.3.0
google-genai>=1.0
httpx[socks]>=0.25
python-dotenv>=1.0
sherpa-onnx>=1.10
piper-tts>=1.2
