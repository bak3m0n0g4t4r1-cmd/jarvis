# Зависимости «Джарвиса». Дублируют dependencies из pyproject.toml для удобства.
# Установка проекта целиком: pip install -e .
# ВНИМАНИЕ: точные версии sherpa-onnx, piper-tts и onnxruntime сверять с тем, что
# реально встаёт на Kali N100 (pip show <пакет>, --help, docstrings).

paho-mqtt>=2.1,<3
pyyaml>=6,<7
numpy>=1.26
sounddevice>=0.4.6
onnxruntime>=1.16
tokenizers>=0.15
sherpa-onnx>=1.10
piper-tts>=1.2
